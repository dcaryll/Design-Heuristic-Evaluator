{
  "name": "design-evaluator",
  "version": "1.0.0",
  "description": "AI-powered UX design evaluation tool with Nielsen's heuristics and modern design system principles",
  "main": "backend/main.py",
  "scripts": {
    "install": "cd backend && python -m pip install -r requirements.txt && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn main:app --reload",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "keywords": [
    "ux",
    "design",
    "evaluation",
    "heuristics",
    "ai",
    "design-system"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.8.0"
  }
}